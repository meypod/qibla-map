/** @type {import('tailwindcss').Config} */
// Content paths come from @nuxtjs/tailwindcss, which scans the app directory;
// setting them here would only shadow that with something less complete.
export default {
  theme: {
    extend: {},
  },
  plugins: [],
};
